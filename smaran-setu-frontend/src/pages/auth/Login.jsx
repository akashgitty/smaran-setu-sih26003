import { useState } from 'react'
import { Link, useNavigate } from 'react-router-dom'
import Logo from '../../components/common/Logo'
import Button from '../../components/common/Button'
import { useAuth } from '../../context/AuthContext'
export default function Login(){const [role,setRole]=useState('user'); const [email,setEmail]=useState(''); const [password,setPassword]=useState(''); const {login}=useAuth(); const navigate=useNavigate(); const submit=e=>{e.preventDefault(); login(role); navigate(role==='user'?'/user/home':'/caregiver/dashboard')}; return <main className="min-h-screen bg-[#f7f8f5] px-4 py-10"><div className="mx-auto max-w-md"><Logo/><div className="card mt-8 p-7"><h1 className="text-2xl font-bold text-[#17345f]">Welcome back</h1><p className="mt-2 text-slate-500">Choose your account type and continue.</p><div className="mt-6 grid grid-cols-2 gap-2 rounded-2xl bg-slate-100 p-1"><button onClick={()=>setRole('user')} className={`rounded-xl p-3 font-bold ${role==='user'?'bg-white shadow text-[#17345f]':'text-slate-500'}`}>User</button><button onClick={()=>setRole('caregiver')} className={`rounded-xl p-3 font-bold ${role==='caregiver'?'bg-white shadow text-[#17345f]':'text-slate-500'}`}>Caregiver</button></div><form onSubmit={submit} className="mt-6 space-y-4"><div><label className="mb-2 block text-sm font-bold">Email</label><input className="input" value={email} onChange={e=>setEmail(e.target.value)} type="email" placeholder="you@example.com" required/></div><div><label className="mb-2 block text-sm font-bold">Password</label><input className="input" value={password} onChange={e=>setPassword(e.target.value)} type="password" placeholder="••••••••" required/></div><Button type="submit" className="w-full">Continue as {role==='user'?'User':'Caregiver'}</Button></form><p className="mt-6 text-center text-sm text-slate-500">New here? <Link className="font-bold text-[#2f8f92]" to="/register">Create an account</Link></p></div></div></main>}
