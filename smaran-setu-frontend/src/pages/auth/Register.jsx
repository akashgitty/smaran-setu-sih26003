import { Link, useNavigate } from 'react-router-dom'
import { useState } from 'react'
import Logo from '../../components/common/Logo'
import Button from '../../components/common/Button'
import { useAuth } from '../../context/AuthContext'
export default function Register(){const [role,setRole]=useState('user');const [name,setName]=useState('');const {login}=useAuth();const navigate=useNavigate();return <main className="min-h-screen bg-[#f7f8f5] px-4 py-10"><div className="mx-auto max-w-md"><Logo/><div className="card mt-8 p-7"><h1 className="text-2xl font-bold text-[#17345f]">Create your account</h1><p className="mt-2 text-slate-500">This prototype saves your selected role locally.</p><div className="mt-6 grid grid-cols-2 gap-3"><button onClick={()=>setRole('user')} className={`rounded-2xl border p-4 text-left ${role==='user'?'border-[#2f8f92] bg-[#e8f2f0]':'border-slate-200'}`}><b>User</b><span className="mt-1 block text-sm text-slate-500">For the elderly user</span></button><button onClick={()=>setRole('caregiver')} className={`rounded-2xl border p-4 text-left ${role==='caregiver'?'border-[#2f8f92] bg-[#e8f2f0]':'border-slate-200'}`}><b>Caregiver</b><span className="mt-1 block text-sm text-slate-500">For family or helper</span></button></div><form className="mt-6 space-y-4" onSubmit={e=>{e.preventDefault();login(role);navigate(role==='user'?'/user/home':'/caregiver/dashboard')}}><input className="input" placeholder="Full name" value={name} onChange={e=>setName(e.target.value)} required/><input className="input" placeholder="Email" type="email" required/><input className="input" placeholder="Create password" type="password" required/><Button type="submit" className="w-full">Create account</Button></form><p className="mt-6 text-center text-sm text-slate-500">Already have an account? <Link className="font-bold text-[#2f8f92]" to="/login">Login</Link></p></div></div></main>}
