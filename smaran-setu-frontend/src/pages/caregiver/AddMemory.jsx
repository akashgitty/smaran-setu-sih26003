import { useState } from 'react'
import { useNavigate } from 'react-router-dom'
import PageTitle from '../../components/common/PageTitle'
import Button from '../../components/common/Button'
export default function AddMemory(){const [saved,setSaved]=useState(false);const navigate=useNavigate();return <><PageTitle title="Add a Memory" subtitle="Create a familiar memory that can later power personalized activities."/><div className="card max-w-2xl p-7"><form className="space-y-5" onSubmit={e=>{e.preventDefault();setSaved(true);setTimeout(()=>navigate('/caregiver/memories'),700)}}><div><label className="mb-2 block text-sm font-bold">Memory title</label><input className="input" placeholder="e.g. Family" required/></div><div><label className="mb-2 block text-sm font-bold">Person / place / object</label><input className="input" placeholder="e.g. Rajesh" required/></div><div><label className="mb-2 block text-sm font-bold">Relationship or category</label><select className="input"><option>Family</option><option>Friend</option><option>Place</option><option>Song</option><option>Important date</option></select></div><div><label className="mb-2 block text-sm font-bold">Memory story</label><textarea className="input min-h-32" placeholder="Write a short, warm description…"/></div><div><label className="mb-2 block text-sm font-bold">Photo</label><input className="input" type="file" accept="image/*"/></div><Button type="submit" className="w-full">{saved?'Saved ✓':'Save Memory'}</Button></form></div></>}
